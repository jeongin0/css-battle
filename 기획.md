# CSS 배틀 — 기획서

> 바이브코딩 전달용 기획 문서. 기획 의도 · 페이지 명세 · 핵심 로직 · 디자인 시스템 · 기술 스택을 한 번에 정리.
> 기술 스택은 원본 기획서(React 제안)에서 **바닐라 JS 기준으로 수정**됨. (사유: [코드규칙.md](./코드규칙.md) 참고)

---

## 1. 프로젝트 개요

**서비스명(가제)**: CSS 배틀

**한 줄 정의**: CSS 선택자와 캐스케이드를 **타이핑·진단·재현** 세 가지 실전 훈련으로 몸에 익히는 학습 도구

> 포지셔닝(2026-08-31): 초기 기획은 "specificity 전문"이었으나, 배틀을 특이도 대결에서 **시안 재현(모작) 연습**으로 바꾸면서 "CSS 실전 감각 훈련"으로 범위를 넓혔다. 특이도/캐스케이드는 CSS 디버그 모드가 중심으로 다루고, 배틀은 셀렉터 위생 체크리스트로 그 감각을 이어받는다.

**문제 정의**
CSS는 텍스트로 설명하면 추상적이라, 입문자는 원리를 이해하지 못한 채 `!important`로 땜빵하는 경우가 많고, 실무자도 충돌이 생기면 감으로 디버깅하는 경우가 많다.

**핵심 가설**
"개념 설명"보다 "손으로 쳐보고, 충돌을 직접 진단하고, 시안을 직접 재현해보는" 반복 학습이 체화 속도를 높인다.

**타겟**
- 주 타겟: CSS 개념이 헷갈리는 입문/주니어 학습자
- 부 타겟: 실무 CSS 충돌 디버깅에 시간 쓰는 현직 개발자

**기술 방향**
서버·DB 없이 **순수 프론트엔드로 완결**. 데이터는 localStorage 기반, 문제 생성·정답 검증· 계산 모두 클라이언트 로직으로 처리. **번들러/프레임워크 없이 바닐라 JS + 정적 파일**로 구현한다.

---

## 2. 학습 사이클 구조

| 단계 | 기능 | 역할 |
|---|---|---|
| 체화 (1단계) | 타자연습 모드 | 선택자를 손으로 반복해서 익히고, 각 선택자가 무엇을 고르는지 툴팁으로 학습 |
| 원리 (2단계) | CSS 디버그 모드 | 앱이 낸 CSS 충돌/버그를 스스로 진단하고, !important 없이 직접 고쳐서 검증 |
| 실전 재현 (3단계) | 배틀 모드 | 디자인 시안을 CSS로 0부터 재현(모작). 겹쳐보기·예시 정답으로 자기 확인, 셀렉터 위생은 체크리스트 |
| 리텐션 | 퀘스트/스탬프 + 마이페이지 칭호 | 매일 돌아오게 만드는 장치 |


---

## 3. 사이트맵 (7 화면)

```
00 랜딩 (3스텝 코스 안내 + 리텐션 스트립 + 모드 데모 + 안심 요소)
 └─ 01 타자연습 (STAGE 01)
 └─ 02 CSS 디버그 (STAGE 02)
 └─ 03 배틀 (STAGE 03)
 └─ 04 오늘의 퀘스트 / 스탬프
 └─ 05 마이페이지 (칭호 · 스트릭 · 스탬프)
```

라우팅은 해시 기반 SPA(`#typing`, `#diagnose`, `#battle`, `#quest`, `#me`)로 구현하고, `index.html` 하나에서 페이지 모듈을 교체 렌더링한다. 헤더 오른쪽에 현재 칭호를 표시하는 마이페이지 링크가 있다. (자세한 구조는 [코드규칙.md](./코드규칙.md) 참고)

> 리포트(성장 통계 + 도감)는 폐기(2026-08-31). 데이터 영속성 부담이 커서 스코프에서 제외. 관련 파일·주석 모두 삭제됨.

---

## 4. 페이지별 상세 명세

### 00. 랜딩

| 요소 | 내용 |
|---|---|
| 헤드라인 | "분명 스타일 줬는데, 왜 안 먹히지?" 공감형 카피 |
| 기능 미리보기 | 배틀 / 타자연습 / CSS 디버그 3개 카드 |
| 기대 효과 섹션 | "CSS 충돌 원인을 스스로 진단", "!important 의존도 감소", "캐스케이드 개념 이해" |
| CTA | "지금 배틀 시작" → 01 화면 이동 |

### 01. 배틀 모드 (시안 + HTML → CSS 작성 → 정확도·정밀도 채점)

**개요**: 완성된 UI 디자인 **시안**과, 스타일이 거의 없는 **HTML 구조**가 주어진다. 사용자는 시안과 똑같이 보이도록 **CSS를 0부터 작성**한다. HTML은 수정할 수 없다. 시안은 카드에 한정하지 않고 헤더·푸터·리스트·내비·히어로·가격표·폼 등 모든 UI 패턴을 다룬다.

**시안의 퀄리티**: HTML/CSS로 렌더하지만 **실제 프로덕트 수준으로 완성도 있게** 만든다(그림자·타이포·여백·정렬까지). 정답 CSS는 공개하지 않고 렌더 결과만 보여준다. 시안은 크게, 눈에 잘 보이게 배치한다.

**색상 추출 (Figma 스타일)**: 시안에서 색을 딸 수 있어야 한다.
- 시안의 핵심 색을 **스와치 팔레트**로 표시 → 클릭 시 hex를 클립보드 복사 / 커서 위치에 삽입
- **스포이드 버튼** → EyeDropper API로 화면 어디든 클릭해 색 추출 (최신 Chrome/Edge)
- 추출한 hex를 바로 CSS 입력창에 넣을 수 있게 한다

**채점하지 않는다 (2026-08-31 변경)**: CSS는 정답이 하나가 아니라서 시안 일치도를 점수로 매기지 않는다. 대신:

1. **자기 확인**: "현재 렌더링" 위에 목표 시안을 **겹쳐보기**(투명도 슬라이더)로 올려 색·간격·정렬을 눈으로 맞춘다.
2. **힌트 버튼**: 예시 정답 CSS를 **한 줄씩** 공개(`3 / 8` 카운터). 다 열면 전체 정답.
3. **예시 정답**: 완료 시 내 CSS와 예시 정답을 나란히. "이대로일 필요는 없다"고 명시.
4. **셀렉터 위생 체크리스트** (점수 아님, 통과/주의만): `!important` 없이 해결 / ID 선택자 미사용 / `*` 미사용 / 죽은 규칙 없음 / 컴포넌트 루트부터 시작 / 3단계 이하. + 규칙별 특이도 표(ID·클래스·태그 칸). → 이 체크리스트가 사이트의 특이도 학습을 배틀에서 이어받는 고리.

**"배틀"의 의미**: 사람 대 사람이 아니라 **"디자인 시안과의 1:1"**(= 모작/클론코딩 연습). 가상 상대·개인 기록·승패 없음. 스톱워치로 경과 시간만 참고로 잰다. 완료를 누르면 그날 활동량(`dayStats.battle`)에 1판 반영(퀘스트용).

**문제별 툴팁**: 각 시안에 "이 시안에서 연습하는 것" 한 줄(`p.tip`)을 결과 화면에 표시.

**난이도 탭**: 초급 / 중급 / 고급 — 컴포넌트 복잡도(요소 수, 레이아웃, 상태 클래스) 기준. 난이도별 풀 15개, **직전 문제 제외 랜덤**, 풀을 다 돌면 재셔플. 컴포넌트 타입(구조)은 중복 없이 구성한다. 아이콘이 필요한 시안은 문제 HTML에 인라인 `<svg>`로 포함한다.
- 초급: 단순 카드·버튼·뱃지 (색/폰트/여백, 요소 3~5개)
- 중급: 내비·리스트·배너 (flex, 상태 클래스, 형제 구분)
- 고급: 헤더·가격표·히어로 (복합 레이아웃, 의사클래스, 반응 상태)

**화면 구성**
- 상단: 난이도 탭 + **[FIGHT(시작) / 정지 / 완료] 버튼**을 한 줄에. **난이도 탭은 시작 후에도 눌러 새 문제를 받을 수 있다**(진행 중 문제는 초기화).
- **FIGHT 클릭 = 시작**. 시작 전 시안·HTML 미리보기 가능, 시작 시 스톱워치 + 에디터 활성화 + 힌트 상자 표시. "정지" = 기록 없이 초기화.
- CSS 입력창은 textarea에 코드 에디터 편의 동작(괄호 자동완성, 자동 들여쓰기, Tab). (`components/cssEditor.js`)
- 좌측: HTML 구조 + 색상 팔레트/스포이드 + 목표 시안(크게).
- 우측: "현재 렌더링"(실시간) + **시안 겹쳐보기 토글·투명도 슬라이더** + CSS 입력창 + **힌트 상자**(예시 정답 한 줄씩).
- 완료 시: 소요시간, 문제 툴팁, 겹쳐보기 안내, **셀렉터 위생 체크리스트** + 특이도 표, 내 CSS vs 예시 정답.
- 초보자용 비유 설명 문구 (예: "ID 칸부터 비교합니다. 낮은 칸이 아무리 많아도 높은 칸 하나를 못 이깁니다.")

### 02. 타자연습 모드

**목표**: CSS 선택자를 **빠르고 정확하게 타이핑**하는 손 훈련. 스스로 작문하는 게 아니라, 주어진 문자열을 그대로 친다.

- 난이도 구분 없이 **한 스트림**. `typingDrills.js`의 큐레이션 풀에서 랜덤 출제(직전 문제 제외).
- 문제는 **복합 선택자 이상**만 낸다 — 클래스 하나 같은 너무 쉬운 건 제외. 조합자(`>`, `+`, `~`), 속성 선택자(`[src^="..."]`), 의사클래스/의사요소(`:nth-child`, `:not`, `::before`), 다중 선택자(`a, b`), 선언부 포함 규칙(`{ ... }`)까지 섞는다.
- 화면 중앙에 목표 문자열, 그 아래 입력창. **글자 단위 맞음/틀림 실시간 하이라이트**.
- 목표 문자열 아래에 **툴팁 한 줄** — 그 선택자가 무엇을 고르는지(`typingDrills.js`의 `{text, tip}`). "체화" 목적의 핵심.
- **정확히 일치해야만** 다음으로(Enter). 다르게 친 채로 Enter → 안내 문구 + **입력창 흔들림**, 콤보 리셋.
- 통계: 타수, 정확도(%), 콤보. 5문제마다 완료 수·최고 콤보 요약 문구. 정확도는 그날 최고값을 `dayStats.typingAcc` 에 반영(퀘스트용).
- "다음 (건너뛰기)" 버튼(콤보 리셋).

### 03. CSS 디버그 모드 (충돌/버그 상황 제시 → 원인 진단 + 최소 수정 처방 → 해설)

**개요**: 사용자가 코드를 붙여넣는 게 아니라, 앱이 현실적인 CSS 버그 상황을 출제한다. "이 요소가 X여야 하는데 Y로 나온다 — 왜인가, 그리고 최소 수정으로 고쳐라." 개발자도구는 "무엇이 이겼나 / 무슨 값이 적용됐나"까지만 답하지만, 이 모드는 원인을 짚고 최소 수정을 고르는 훈련을 시킨다.

**왜 붙여넣기 분석기가 아닌가**: 학습 중인 사용자는 붙여넣을 실무 코드가 없고, 실무 버그를 만난 사람은 그 순간 개발자도구를 연다. 붙여넣기 도구는 개발자도구의 부정확한 하위호환이라, 능동적 문제 풀이로 전환한다.

**페이지 상단 안내**: 실무 유지보수의 순서를 짧게 설명한다 — ① 이렇게 보여야 하는데 → ② 스타일 줬는데 안 먹힘 → ③ 왜 안 먹히나(원인) → ④ !important 없이 최소 수정(해결).

**문제 세 갈래** (`diagnoseChallenges.js` 큐레이션 풀 ~26개, shuffle-bag 무한 순환. 목표 30개):

1. **`cascade`** — "여러 규칙 중 어느 게 이기나": 명시도 / `!important` 층 / 소스 순서. (9문제)
   - Q1: 이기는 규칙 + 이유(객관식). 오답 보기에 "클래스를 더 써서" 같은 흔한 오해 포함.
   - 해설: `cascadeReplay.js` 로 정답 규칙·이유 산출 + 정지된 명시도 비교표.
2. **`behavior`** — "속성은 적용됐는데 효과가 안 나는": 쌓임 맥락, 컨테이닝 블록, overflow 클리핑, 마진 상쇄, % 높이, object-fit. (12문제)
   - Q1: "왜 효과가 안 나나?"(객관식, `causes`). 검증: `elementFromPoint` / `getBoundingClientRect`.
3. **`inherit`** — "부모 색/폰트를 정했는데 자식(a·button·input)만 안 물려받는다": 상속은 자식에 값이 없을 때만. (5문제)
   - 검증: `computedEquals` (probe 요소의 computed 값과 대상 비교).

`!important` 처방은 behavior·inherit 유형에선 **항상 오답**.

**2단계 처방 = 객관식 + 직접 타이핑 (2026-08-31, 옵션 B)**:
- 객관식 보기(best/works/nope)는 그대로 두고, 그 아래 **`<details>` 직접 고쳐보기** textarea 추가.
- textarea에 내용이 있으면 그걸로 채점: `buildCss(rules) + 사용자 CSS` 를 iframe에 렌더 → 기존 `verifyFix` 로 검증. `!important` 안 쓰고 효과 나면 best, 쓰면 works, 안 나면 nope.
- "적용해서 검증" 버튼으로 제출 전 즉시 확인 가능.
- 객관식/타이핑 중 하나만 하면 제출 가능.

**공통**: 결과 = 왼쪽에 개념별 고정 참고(`REFERENCE[concept]`, inherit 포함), 오른쪽에 "이번 문제 해설" + 수정 전/후 미니 렌더.

**채점**: 진단 + 처방(best 2 / works 1 / nope 0). 둘 다 만점 시 콤보 +1. 해결 시 `dayStats.diagnose` +1(퀘스트용).

**세트 단위**: 3문제를 한 세트로 진행한다(상단에 "이번 세트 N / 3" 표시). 세트가 끝나면 진단·처방 요약을 보여주고 "새 문제 3개 받기"로 이어진다. 다음 세트는 **아직 안 나온 문제**로 채운다 — 풀 전체를 한 바퀴 돌기 전엔 같은 문제가 반복되지 않는다(shuffle-bag). 풀을 다 돌면 재셔플해 무한히 계속된다.

**통계**: 이번 세트 진행도 / 누적 해결 수 / 진단 정확도 % / 콤보.

### 04. 오늘의 퀘스트 / 스탬프

- 오늘의 퀘스트 리스트 (3개 슬롯 = 배틀/타자/디버그, 체크리스트형)
  - 목표치가 날짜마다 로테이션 (배틀 시안 2~4개 / 타자 정확도 85~95% / 디버그 1~3문제). 슬롯 id는 고정
  - 완료 여부는 그날 `dayStats` 로 판정. **퀘스트 페이지에 들어올 때 재평가**한다 — 다른 페이지에서 실시간 알림은 하지 않는다(의도).
- 연속 출석 스트릭 배지 (사이트에 하루 한 번 접속 = 이어짐, 하루 비면 리셋)
- 스탬프 카드 (이번 달 전체 달력 + 옆달 며칠 미리보기). 셀 상태: 금색 테두리=접속만 / 초록 테두리=퀘스트 일부 / 초록 배경+★=3개 전부(=스탬프). 범례는 그리드 아래
- 다음 칭호까지 남은 스탬프 수 안내. **칭호 목록·획득 현황은 마이페이지(05)로 이동**.

### 05. 마이페이지 (`#me`)

- 헤더 오른쪽 "마이페이지 / (현재 칭호)" 링크로 진입. 현재 칭호는 `main.js` 가 헤더에 항상 렌더.
- 현재 칭호(가장 높이 획득한 것, 없으면 "신입 도전자") 크게 + 🔥 연속/최장/스탬프 수/총 접속일.
- 칭호 목록 4단계(스탬프 3·7·14·30개)와 획득/잠김 상태. 다음 칭호까지 남은 스탬프.
- 보상은 **칭호 표시가 전부** — 기능 언락 없음(관리 부담 회피). 성취감 장치로만.

---

## 5. 핵심 로직 명세

### 5-1. 계산 알고리즘

```
입력: CSS 셀렉터 문자열
처리:
  1. 정규식으로 토큰 분리
     - 인라인 스타일 여부 (해당 시 최상위 우선)
     - #id 패턴 개수
     - .class, [attr], :pseudo-class 패턴 개수
     - 태그명, ::pseudo-element 패턴 개수
  2. !important 포함 여부 별도 플래그로 분리 (보다 상위 우선순위)
  3. (인라인, ID, 클래스, 태그) 튜플 반환
출력: 튜플 + important 플래그
비교 로직: important 우선 → 튜플 앞자리부터 사전식 비교
```

### 5-2. CSS 디버그 로직 (CSS 디버그 모드)

```
입력: 큐레이션 문제 = {
  type: 'cascade' | 'behavior', concept, HTML, 규칙 배열(순서 유지), 대상 셀렉터,
  cascade: 기대 속성/값 + 정답(이기는 규칙 index + 이유),
  behavior: causes(원인 보기 + correct 플래그) + verify(검증 방식) + teach(해설 문구),
  처방 후보: { 변환 op, 종류 best|works|nope }
}
처리:
  1. 원래 CSS+HTML을 iframe에 렌더, 대상 요소를 querySelectorAll 로 확정
  2. cascade → core/cascadeReplay.js 로 이기는 규칙·이유 산출 + 정지 명시도 비교표
     behavior → 개념별 고정 참고(REFERENCE[concept]) + 원인·처방 해설 문구
  3. 처방 검증: 후보의 변환(selector 교체 / prefix / !important / 순서 이동 /
     add-decl / remove-decl / replace-decl)을 규칙 배열에 적용, 새 CSS를 iframe에 렌더 →
     - cascade: getComputedStyle(대상)[속성] === 기대값
     - behavior: verify 방식대로 검증 (onTop=elementFromPoint / insideParent·gap·boxSize=getBoundingClientRect / hasHeight / notClipped)
     라벨이 아니라 실제 렌더 결과로 판정.
  4. 채점: 진단(cascade는 규칙+이유, behavior는 원인) + 처방(best 2 / works 1 / nope 0) → 점수·콤보
출력: 개념별 고정 참고 + 이번 문제 해설(정답·이유·처방) + 수정 전후 미니 렌더
비고: 붙여넣기 분석이 아닌 출제형. 상속(inherit)은 2026-08-31에 출제 범위로 편입. @media·@layer 는 여전히 제외.
```

### 5-3. 배틀 모드 로직 (채점 없음)

```
입력: 사용자 CSS 전체, 문제의 HTML, 예시 정답 CSS(problem.answerCss), 문제 툴팁(problem.tip)
처리:
  1. 실시간: 사용자 CSS를 iframe(previewDoc)에 렌더 → "현재 렌더링".
  2. 겹쳐보기: 목표 시안(obfuscatedShown)을 같은 자리에 절대배치로 올리고 투명도 조절.
  3. 힌트: answerCss 를 줄 단위로 잘라 "힌트" 버튼마다 다음 줄 공개.
  4. 완료 시 selectorHygiene(사용자CSS, 렌더 문서, problem.root):
     - 규칙 파싱(cascade.parseStylesheet) → 규칙별 특이도(5-1)
     - 체크리스트(통과/주의): !important / ID 선택자 / `*` / 죽은 규칙 / 루트 앵커 / 3단계 초과
  5. addBattleClear() → dayStats.battle += 1
출력: 소요시간 + 툴팁 + 겹쳐보기 안내 + 체크리스트 + 특이도 표 + 내 CSS vs 예시 정답
비고: 시안 일치도는 점수화하지 않는다(정답이 하나가 아니라서). 큐레이션 시안 풀(battleProblems.js)에서 출제.
```

### 5-4. (삭제) 문제 자동 생성

타자연습·배틀 모두 큐레이션 풀(`typingDrills.js` / `battleProblems.js`)에서만 출제한다. 자동 생성·적응형 난이도는 폐기.

### 5-5. 퀘스트 / 스트릭 로직

```
- visitLog(YYYY-MM-DD: true): 접속 스트릭. 오늘 기준 역순 카운트, 하루 비면 리셋.
- dayStats(YYYY-MM-DD: {battle, typingAcc, diagnose}): 그날 활동량. 퀘스트 완료 판정용.
- dailyQuests(): 슬롯 3개(battle/typing/diagnose) 고정, 목표치만 날짜로 로테이션.
- questLog(YYYY-MM-DD: [questId]): 퀘스트 페이지 진입 시 재평가 → 완료분 markQuestDone.
- perfectDays: questLog 에서 3개 모두 완료한 날 수 = 스탬프. 스탬프 3·7·14·30 → 칭호.
```

### 5-6. localStorage 데이터 스키마

```json
{
  "dayStats": {
    "2026-08-31": { "battle": 2, "typingAcc": 92, "diagnose": 3 }
  },
  "questLog": { "2026-08-31": ["battle_3win", "typing_90acc"] },
  "visitLog": { "2026-08-31": true },
  "streak": { "current": 5, "longest": 12 },
  "settings": { "difficulty": "low" }
}
```

> 기록 배열(battleRecords/typingRecords/diagnoseRecords)은 폐기. 리포트가 없어져 누적 기록이 불필요. dayStats 만 유지(퀘스트·스탬프 최소 요건).

---

## 6. 디자인 시스템

**컨셉**: 8bit 아케이드 격투게임 톤. 체력바·VS·KO 연출은 게임 요소로, 코드/ breakdown 영역은 모노스페이스로 실무 도구 느낌 유지.

**컬러 토큰**

| 이름 | 값 | 용도 |
|---|---|---|
| bg | `#150C22` | 배경 |
| panel | `#1E1230` | 카드 배경 1 |
| panel-light | `#2A1B42` | 카드 배경 2 |
| p1 (cyan) | `#00E5FF` | 플레이어1 / 강조 |
| p2 (magenta) | `#FF2E63` | 플레이어2 / 경고 |
| gold | `#FFD23F` | CTA / 보상 / 승리 강조 |
| hp-green | `#4ADE80` | 완료/정답 |
| hp-red | `#FF4757` | 낮은 체력 |
| ink | `#F1EEFA` | 기본 텍스트 |
| ink-dim | `#9084AE` | 보조 텍스트 |

**타이포그래피**
- 본문/기본: `Pretendard` (헤딩·버튼·배지는 물론 셀렉터 코드, DOM 트리, 특이도 breakdown 표 등 코드/데이터 영역까지 전 영역 기본값)
- 포인트: `Press Start 2P` (픽셀 폰트. 로고, STAGE 라벨, FIGHT 버튼, 통계 수치, 헤더 칭호 등 임팩트가 필요한 순간에만 절제해서 사용)

**시그니처 요소**: 시안 겹쳐보기 (배틀), 실시간 하이라이트 타이핑창 + 선택자 툴팁 (타자연습), 명시도 비교표 (CSS 디버그), 스탬프 달력 카드 (퀘스트)

---

## 7. 기술 스택 (바닐라 JS 기준으로 수정)

> 원본 기획서는 React를 제안했으나, 작성자가 React/Vue 경험이 전혀 없어 **본인이 이해하고 설명할 수 있는 코드**를 유지하는 것을 우선하여 바닐라 JS로 전환.

- **프레임워크**: 없음 — 순수 HTML/CSS/JS (ES Modules), 번들러 없이 정적 파일로 구동
- **라우팅**: 해시 기반 커스텀 SPA 라우터 직접 구현 (`hashchange` 이벤트 + 페이지 모듈 스왑)
- **상태관리**: 단일 전역 `store` 객체 (js/store.js) — localStorage와 동기화, 변경 시 구독자에게 알림
- **정답 검증**: 브라우저 네이티브 `document.querySelectorAll` + iframe 렌더 후 `getComputedStyle`/`getBoundingClientRect`
- **데이터 저장**: localStorage (dayStats/questLog/visitLog/streak/settings 만). 영속성·동기화·내보내기는 스코프 밖.
- **폰트**: `component.css`의 `@import` 두 줄 — Google Fonts(`Press Start 2P`) + Pretendard CDN(`orioncactus/pretendard`)
- **지도/외부 API**: 해당 없음 (전 기능 클라이언트 완결)

---

## 8. 남은 작업 / 확장 여지

- CSS 디버그 문제 26개 → **30개**로 마저 확장 (cascade/behavior/inherit).
- 배틀 문제별 툴팁·예시 정답 품질 점검.
- 모바일(≤1024px) 레이아웃 실제 기기 확인.
- (선택) 학습 경로를 랜딩 3스텝 이상으로 — 코스 완주 추적 등. 단 localStorage 확장은 신중히.
