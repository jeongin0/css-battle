# CSS 배틀 — 기획서

> 바이브코딩 전달용 기획 문서. 기획 의도 · 페이지 명세 · 핵심 로직 · 디자인 시스템 · 기술 스택을 한 번에 정리.
> 기술 스택은 원본 기획서(React 제안)에서 **바닐라 JS 기준으로 수정**됨. (사유: [코드규칙.md](./코드규칙.md) 참고)

---

## 1. 프로젝트 개요

**서비스명(가제)**: CSS 배틀 (CSS Specificity Battle)

**한 줄 정의**: CSS(specificity) 개념을 대결·타이핑·실전 진단으로 체화시키는 학습 도구

**문제 정의**
CSS는 텍스트로 설명하면 추상적이라, 입문자는 원리를 이해하지 못한 채 `!important`로 땜빵하는 경우가 많고, 실무자도 충돌이 생기면 감으로 디버깅하는 경우가 많다.

**핵심 가설**
"개념 설명"보다 "대결 결과를 직접 보고, 손으로 쳐보고, 실전 코드에 적용해보는" 반복 학습이 체화 속도를 높인다.

**타겟**
- 주 타겟: CSS 개념이 헷갈리는 입문/주니어 학습자
- 부 타겟: 실무 CSS 충돌 디버깅에 시간 쓰는 현직 개발자

**기술 방향**
서버·DB 없이 **순수 프론트엔드로 완결**. 데이터는 localStorage 기반, 문제 생성·정답 검증· 계산 모두 클라이언트 로직으로 처리. **번들러/프레임워크 없이 바닐라 JS + 정적 파일**로 구현한다.

---

## 2. 학습 사이클 구조

| 단계 | 기능 | 역할 |
|---|---|---|
| 이해·실전 응용 | 배틀 모드 | 구조+디자인 시안을 보고 CSS로 직접 재현하며 개념을 결과물로 검증 |
| 체화 | 타자연습 모드 | 손으로 반복해서 익힘 |
| 실전 적용 | 진단 모드 | 실제 프로젝트 코드에 적용 |
| 리텐션 | 퀘스트/스탬프 | 매일 돌아오게 만드는 장치 |
| 결과 증명 | 성장 리포트 | 학습 효과를 데이터로 시각화 |

---

## 3. 사이트맵 (7 화면)

```
00 랜딩
 └─ 01 배틀 (구조+디자인 시안 → CSS 작성 → 판정 결과)
 └─ 02 타자연습 모드
 └─ 03 실전 진단 모드
 └─ 04 오늘의 퀘스트 / 스탬프
 └─ 05 성장 리포트 (+ 도감 탭)
```

라우팅은 해시 기반 SPA(`#battle`, `#typing`, `#diagnose`, `#quest`, `#report`)로 구현하고, `index.html` 하나에서 페이지 모듈을 교체 렌더링한다. (자세한 구조는 [코드규칙.md](./코드규칙.md) 참고)

---

## 4. 페이지별 상세 명세

### 00. 랜딩

| 요소 | 내용 |
|---|---|
| 헤드라인 | "분명 스타일 줬는데, 왜 안 먹히지?" 공감형 카피 |
| 기능 미리보기 | 배틀 / 타자연습 / 진단 3개 카드 |
| 기대 효과 섹션 | "CSS 충돌 원인을 스스로 진단", "!important 의존도 감소", "캐스케이드 개념 이해" |
| CTA | "지금 배틀 시작" → 01 화면 이동 |

### 01. 배틀 모드 (시안 + HTML → CSS 작성 → 정확도·정밀도 채점)

**개요**: 완성된 UI 디자인 **시안**과, 스타일이 거의 없는 **HTML 구조**가 주어진다. 사용자는 시안과 똑같이 보이도록 **CSS를 0부터 작성**한다. HTML은 수정할 수 없다. 시안은 카드에 한정하지 않고 헤더·푸터·리스트·내비·히어로·가격표·폼 등 모든 UI 패턴을 다룬다.

**시안의 퀄리티**: HTML/CSS로 렌더하지만 **실제 프로덕트 수준으로 완성도 있게** 만든다(그림자·타이포·여백·정렬까지). 정답 CSS는 공개하지 않고 렌더 결과만 보여준다. 시안은 크게, 눈에 잘 보이게 배치한다.

**색상 추출 (Figma 스타일)**: 시안에서 색을 딸 수 있어야 한다.
- 시안의 핵심 색을 **스와치 팔레트**로 표시 → 클릭 시 hex를 클립보드 복사 / 커서 위치에 삽입
- **스포이드 버튼** → EyeDropper API로 화면 어디든 클릭해 색 추출 (최신 Chrome/Edge)
- 추출한 hex를 바로 CSS 입력창에 넣을 수 있게 한다

**채점 2축**

1. **정확도 (시안 일치)**: 사용자 CSS를 적용한 렌더의 `getComputedStyle`을 **컴포넌트 전체 요소**에 대해 시안 값과 비교한다. 목표 요소의 색·크기·간격·정렬은 물론, **"건드리면 안 되는" 요소가 바뀌었는지(스타일 누수)**도 정확도에 반영한다. 색은 RGB 근사, 길이는 px 오차 허용. 일정 기준(예: 90%) 이상이어야 클리어.

2. **정밀도 (셀렉터 위생)**: 셀렉터를 얼마나 촘촘하고 깔끔하게 잡았는지 100점 기준으로 점수화한다.
   - 감점: `!important`, 인라인 스타일, `*`/전체 선택자, 아무 것도 안 잡는 죽은 규칙, 3단계 초과 깊은 체이닝, 불필요한 과잉 특이도(클래스로 충분한데 ID 사용 등), 같은 선언 중복
   - 유지: 최소 특이도로 의도한 요소만 정확히 선택
   - 규칙별 특이도 breakdown `(인라인, ID, 클래스, 태그)` 표로 근거 표시

**대결 방식** (문제는 동일, 성공 판정만 다름)
- **솔로 타임어택**: 실패 개념 없음(정확도 기준 넘을 때까지). 스톱워치로 시작~완료 경과 시간 기록.
- **가상 상대(고스트) 대결**: 문제마다 정해진 **기준 점수(par)** = 정답 CSS의 정밀도 점수. par가 "촘촘하게 짠 남의 코드"이며 이걸 "적"으로 삼는다. 클리어 + 내 정밀도 ≥ par → 상대 KO, 미만 → 내 HP 소진 연출. HP바 2개 / KO 스탬프.

**난이도 탭**: 초급 / 중급 / 고급 — 컴포넌트 복잡도(요소 수, 레이아웃, 상태 클래스) 기준. 난이도별 풀 4~6개, **직전 문제 제외 랜덤**, 풀을 다 돌면 재셔플.
- 초급: 단순 카드·버튼·뱃지 (색/폰트/여백, 요소 3~5개)
- 중급: 내비·리스트·배너 (flex, 상태 클래스, 형제 구분)
- 고급: 헤더·가격표·히어로 (복합 레이아웃, 의사클래스, 반응 상태)

**화면 구성**
- 상단: 난이도 탭 + 대결 방식 + **[FIGHT(시작) / 정지 / 완료] 버튼**을 한 줄에 배치
- **FIGHT 클릭 = 시작** (자동 시작 없음). 시작 전 시안·HTML 미리보기 가능, 시작 시 스톱워치 + 에디터 활성화. "정지" = 기록 없이 초기화.
- 좌측: 목표 시안(크게) + 색상 팔레트/스포이드 + HTML 구조. HTML 표시와 CSS 입력창 높이를 맞춘다.
- 우측: CSS 입력창 + "현재 렌더링" 미리보기(실시간, 크게). 시작 상태 = 무스타일 HTML.
- 완료 시: 정확도 %, 정밀도 점수(+ par 비교), 누수·감점 항목 목록, 특이도 표, 소요시간
- 초보자용 비유 설명 문구 (예: "ID는 학생증처럼 유일해서 클래스 여러 개보다 셉니다")
- "솔로 vs 가상 상대 차이" 요약을 화면에 노출
- "전적에 저장" 버튼 → 05 성장 리포트 데이터로 누적

### 02. 타자연습 모드

- 상단: DOM 트리 스니펫 표시 (들여쓰기된 HTML)
- 미션 문구: 난이도별 조건 제시
  - 초급: "이 엘리먼트만 선택하는 셀렉터"
  - 중급: " (0,0,2,0) 이상으로 선택"
  - 고급: "P2 셀렉터를 이기는 셀렉터를 즉석에서 작성"
- 타이핑 입력창: 글자 단위 맞음/틀림 실시간 하이라이트
- 통계: 타수(타/분), 정확도(%), 콤보(연속 정답)
- 정답 시 다음 문제 자동 생성 (섹션 5-4 참고)

### 03. 실전 진단 모드

- CSS 코드 블록 붙여넣기 입력창
- "대상 엘리먼트" 지정 (태그/클래스/ID 텍스트 입력)
- 매칭되는 규칙을 우선순위 순으로 랭킹 표시, 각 항목에 승리 사유 표기
- 하단 안내 문구: "근사 계산이며 실제 브라우저와 다를 수 있습니다"

### 04. 오늘의 퀘스트 / 스탬프

- 오늘의 퀘스트 리스트 (3개, 체크리스트형)
  - 예: "배틀 3판 승리", "타자연습 정확도 90%+ 1회", "진단 모드 1회 사용"
- 연속 출석 스트릭 배지
- 스탬프 카드 (주간 달력형 그리드, 완료일마다 도장)
- 다음 보상까지 남은 스탬프 수 안내 (칭호/뱃지 언락)

### 05. 성장 리포트 (+ 도감)

**탭 1: 성장 리포트**
- 총 배틀 수 / 현재 승률 / 학습 일수 요약 카드 3개
- 주차별 승률 추이 막대그래프
- Before/After 비교 (예: "3주 전 40% → 지금 82%")
- 타이핑 속도·정확도 추이 그래프

**탭 2: 도감**
- 셀렉터 유형별 카드 (ID, 클래스, 태그 등) — 값 + 설명
- "이번 주 가장 많이 진 유형" 같은 개인화 인사이트

---

## 5. 핵심 로직 명세

### 5-1. 계산 알고리즘

```
입력: CSS 셀렉터 문자열
처리:
  1. 정규식으로 토큰 분리
     - 인라인 스타일 여부 (해당 시 최상위 우선)
     - #id 패턴 개수
     - .class, [attr], :pseudo-class 패턴 개수
     - 태그명, ::pseudo-element 패턴 개수
  2. !important 포함 여부 별도 플래그로 분리 (보다 상위 우선순위)
  3. (인라인, ID, 클래스, 태그) 튜플 반환
출력: 튜플 + important 플래그
비교 로직: important 우선 → 튜플 앞자리부터 사전식 비교
```

### 5-2. 캐스케이드 랭킹 로직 (진단 모드)

```
입력: CSS 코드 블록, 대상 엘리먼트 조건(태그/클래스/ID)
처리:
  1. CSS 코드를 규칙 단위로 파싱 (정규식 기반)
  2. 각 규칙의 마지막 컴파운드 셀렉터가 대상 조건과 겹치는지로 매칭 판정
     (완전한 브라우저 매칭 엔진 재현이 아닌 "교육용 근사치" — UI에 명시)
  3. 매칭된 규칙들을 정렬:
     important 여부 → 튜플 → 소스 코드 등장 순서(나중 규칙 우선)
출력: 우선순위 순 랭킹 리스트 + 각 항목 승리/패배 사유
```

### 5-3. 배틀 모드 판정 로직

```
입력: 사용자가 작성한 CSS 전체(여러 규칙), 문제의 HTML,
      시안 스펙 = 컴포넌트 요소별 { 검증 속성: 기대값, 허용오차 } + "기본값 유지 요소" 목록
처리:
  1. [정확도] 사용자 CSS를 iframe DOM에 적용 → getComputedStyle로 요소별 검증 속성을
     기대값과 허용오차 내 비교 (색: RGB 근사 / 길이: px 오차 / 레이아웃: 최종 결과만).
     "기본값 유지 요소"가 바뀌었으면 스타일 누수로 감점.
     정확도 = 통과 검증항목 / 전체 검증항목. 기준(예: 90%) 이상이면 클리어.
  2. [정밀도] 사용자 CSS를 규칙 단위로 파싱, 100점에서 감점:
     - !important 사용 / 인라인 스타일 / `*`·전체 선택자 사용
     - 아무 요소도 매칭 못 하는 죽은 규칙
     - 컴파운드 3단계 초과 깊은 체이닝
     - 과잉 특이도(같은 요소를 잡는데 필요보다 높은 특이도) — 5-1로 계산
     - 동일 선언 중복
  3. 대결 방식별 결과
     - 솔로: 클리어까지 스톱워치 경과 시간 기록 (실패 없음)
     - 고스트: 클리어 AND 정밀도 ≥ par → KO / 아니면 HP 소진 (par = 정답 CSS의 정밀도)
출력: 정확도 % + 정밀도 점수(+par) + 누수·감점 항목 목록 + 특이도 breakdown 표 + 소요시간
주의: CSS는 정답이 하나가 아니므로 정확도는 "코드 비교"가 아닌 "렌더링 결과(computed style)
     비교"로만 판정한다. 픽셀 단위가 아닌 근사 판정이며 UI에 이 사실을 명시한다.
     배틀 문제는 자동 생성(5-4)이 아니라 큐레이션된 시안 풀에서 출제한다.
```

### 5-4. 문제 자동 생성 로직 (타자연습 모드 · 배틀 모드 공용)

```
1. DOM 스니펫 생성
   - 큐레이션된 태그/클래스명 풀에서 랜덤 조합
     (예: div, span, h2, p / .card, .title, .active, .btn 등)
   - 난이도에 따라 중첩 깊이 결정 (초급 1~2단계, 고급 3~4단계)
2. 미션 조건 생성 (타자연습 전용 — 배틀은 5-3의 큐레이션 시안 사용)
   - 타자연습 초급: 생성된 DOM 중 임의 엘리먼트 하나를 타겟 지정
   - 타자연습 중급: 목표 값 제시 (예: (0,0,2,0) 이상)
   - 타자연습 고급: 상충하는 상대 규칙 제시 후 "이기는 셀렉터 작성"
3. 정답 검증
   - document.querySelectorAll(입력 셀렉터) 로 실제 타겟 엘리먼트 매칭 여부 확인
     (브라우저 네이티브 엔진 사용 → 진단 모드의 근사치와 달리 100% 정확)
   - 계산 함수(5-1)로 조건 충족 여부 확인
4. 적응형 난이도
   - 최근 N문제 정답률 추적
   - 정답률 높으면 중첩 깊이/조건 강도 상향, 낮으면 하향
```

### 5-5. 퀘스트 / 스트릭 로직

```
- 날짜별 완료 기록을 localStorage에 저장 (YYYY-MM-DD 키)
- 스트릭 계산: 오늘 기준 역순으로 연속된 완료일 카운트, 하루라도 비면 스트릭 리셋
- 스탬프 N개 누적 시 보상(칭호/뱃지) 언락 테이블 별도 관리
```

### 5-6. localStorage 데이터 스키마 (예시)

```json
{
  "battleRecords": [
    {
      "date": "2026-08-25",
      "mode": "ghost",
      "difficulty": "mid",
      "selector": "#main .card h2",
      "wonSpecificity": true,
      "matchedDesign": true,
      "timeSec": 42,
      "result": "win"
    }
  ],
  "typingRecords": [
    { "date": "2026-08-25", "wpm": 312, "accuracy": 96, "combo": 7, "difficulty": "mid" }
  ],
  "questLog": {
    "2026-08-25": ["battle_3win", "typing_90acc"]
  },
  "streak": { "current": 5, "longest": 12 },
  "settings": { "difficulty": "mid" }
}
```

---

## 6. 디자인 시스템

**컨셉**: 8bit 아케이드 격투게임 톤. 체력바·VS·KO 연출은 게임 요소로, 코드/ breakdown 영역은 모노스페이스로 실무 도구 느낌 유지.

**컬러 토큰**

| 이름 | 값 | 용도 |
|---|---|---|
| bg | `#150C22` | 배경 |
| panel | `#1E1230` | 카드 배경 1 |
| panel-light | `#2A1B42` | 카드 배경 2 |
| p1 (cyan) | `#00E5FF` | 플레이어1 / 강조 |
| p2 (magenta) | `#FF2E63` | 플레이어2 / 경고 |
| gold | `#FFD23F` | CTA / 보상 / 승리 강조 |
| hp-green | `#4ADE80` | 완료/정답 |
| hp-red | `#FF4757` | 낮은 체력 |
| ink | `#F1EEFA` | 기본 텍스트 |
| ink-dim | `#9084AE` | 보조 텍스트 |

**타이포그래피**
- 본문/기본: `Pretendard` (헤딩·버튼·배지는 물론 셀렉터 코드, DOM 트리, 특이도 breakdown 표 등 코드/데이터 영역까지 전 영역 기본값)
- 포인트: `Press Start 2P` (픽셀 폰트. FIGHT 버튼 글자, KO 스탬프 등 임팩트가 필요한 순간에만 절제해서 사용 — `.btn-point`, `.ko-stamp`)

**시그니처 요소**: 체력바 + KO 스탬프 (배틀), 실시간 하이라이트 타이핑창 (타자연습), 스탬프 달력 카드 (퀘스트)

---

## 7. 기술 스택 (바닐라 JS 기준으로 수정)

> 원본 기획서는 React를 제안했으나, 작성자가 React/Vue 경험이 전혀 없어 **본인이 이해하고 설명할 수 있는 코드**를 유지하는 것을 우선하여 바닐라 JS로 전환.

- **프레임워크**: 없음 — 순수 HTML/CSS/JS (ES Modules), 번들러 없이 정적 파일로 구동
- **라우팅**: 해시 기반 커스텀 SPA 라우터 직접 구현 (`hashchange` 이벤트 + 페이지 모듈 스왑)
- **상태관리**: 단일 전역 `store` 객체 (js/store.js) — localStorage와 동기화, 변경 시 구독자에게 알림
- **차트**: `<canvas>` API로 직접 그리기 (막대그래프 수준은 라이브러리 불필요)
- **정답 검증**: 브라우저 네이티브 `document.querySelectorAll`
- **데이터 저장**: localStorage
- **폰트**: `common.css`/`components.css`의 `@import` 두 줄로 로드 — Google Fonts CDN(`Press Start 2P`) + Pretendard 공식 CDN(`orioncactus/pretendard`, 전체 굵기 번들)
- **지도/외부 API**: 해당 없음 (전 기능 클라이언트 완결)

---

## 8. 개발 우선순위 제안 (MVP → 확장)

1. **MVP**: 랜딩 + 배틀 모드 솔로 타임어택 버전 (구조+시안 제시 → CSS 작성 → 승패 + 시안 일치 판정, 5-3 로직) + 계산 로직(5-1)
2. **2단계**: 배틀 모드 확장 — 가상 상대(고스트) 대결 모드, HP바/KO 연출 추가
3. **3단계**: 타자연습 모드 + 문제 자동 생성 로직
4. **4단계**: 성장 리포트 (배틀/타자 기록 시각화)
5. **5단계**: 퀘스트/스탬프 시스템
6. **6단계**: 실전 진단 모드 (CSS 파싱 난이도가 있어 최후순위 권장)

이 순서로 가면 매 단계마다 "동작하는 결과물"이 나와서 바이브코딩 진행 중간중간 확인하기 좋음.
