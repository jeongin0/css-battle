# 코드 규칙

> 이 프로젝트는 **바닐라 JS(HTML/CSS/JS, ES Modules)**로 만든다. 번들러·프레임워크 없음.
> 이유: 작성자가 React/Vue를 모르고, 모르는 기술로 만들면 나중에 코드를 스스로 설명·수정할 수 없기 때문. 실용적인 최소 규칙만 정한다.
> 회사 퇴사로 인해 회사에서 사용한 구조를 직접적으로 사용은 못하고 참고하여 사용하기. 회사명 같은 네임은 사용 불가.

---

## 1. 폴더 구조

```
/
├─ index.html              # 진입점. 헤더/공통 레이아웃 + <div id="app"> 하나
├─ css/
│  ├─ reset.css            # 기본 리셋
│  ├─ common.css           # html/body/컨테이너 + 모든 페이지 공통 텍스트(.page-title 등)
│  ├─ layout.css           # 헤더 등 공통 레이아웃
│  ├─ component.css        # 컬러/폰트/간격 변수(:root) + 2개 이상 화면에서 쓰는 재사용 컴포넌트(.btn, .card, .tabs, .health-bar 등)
│  └─ pages/               # 화면 전용 스타일. 화면당 파일 1개 (landing/battle/typing/diagnose/quest/report.css)
├─ js/
│  ├─ main.js              # 앱 진입점: 라우터 초기화, store 초기화
│  ├─ router.js            # SPA 라우터
│  ├─ store.js             # 전역 상태 + localStorage 동기화
│  ├─ core/                # 화면에 종속되지 않는 순수 로직
│  │  ├─ specificity.js    # 5-1 특이도 계산
│  │  ├─ cascade.js        # 규칙 파싱 헬퍼(배틀 채점 공용) + 구 진단 랭킹 로직
│  │  ├─ cascadeReplay.js  # 5-2 판정 리플레이(매칭→!important 층→특이도 자릿수→소스 순서)
│  │  ├─ diagnoseChallenges.js # 5-2 CSS 디버그 문제 풀(shuffle-bag) + 처방 변환/검증
│  │  ├─ battleProblems.js # 5-3 배틀 시안 풀(난이도별) + 렌더 헬퍼
│  │  ├─ battleScore.js    # 5-3 배틀 채점 (정확도 = computed style 비교 / 정밀도 = 셀렉터 위생)
│  │  ├─ problemGenerator.js # 5-4 타자연습 문제 생성
│  │  └─ streak.js         # 5-5 퀘스트/스트릭 계산
│  ├─ pages/                # 화면 단위. 화면당 파일 1개
│  │  ├─ landing.js
│  │  ├─ battle.js
│  │  ├─ typing.js
│  │  ├─ diagnose.js
│  │  ├─ quest.js
│  │  └─ report.js
│  └─ components/           # 여러 화면에서 재사용하는 UI 조각
│     ├─ cssEditor.js       # textarea 코드 에디터 편의 동작 (배틀)
│     ├─ chart.js
│     └─ stampCalendar.js
└─ img/                     # 이미지 등 정적 파일 (폰트는 CDN 사용)
```

- 화면(7개) ↔ `pages/`의 파일이 1:1로 대응된다 (랜딩 포함 6개 파일 + `report.js`가 도감 탭까지 포함).
- 개발은 [기획.md](./기획.md) 8장의 MVP 우선순위(배틀 솔로 → 배틀 고스트 대결 → 타자연습 → 리포트 → 퀘스트 → 진단)를 따르되, 폴더 구조는 미리 위와 같이 잡아두고 순서대로 채운다.

---

## 2. 파일/네이밍 규칙

- 파일명: `camelCase.js` (예: `problemGenerator.js`), CSS는 `kebab-case.css`
- 함수/변수: `camelCase`, 상수(변하지 않는 설정값): `UPPER_SNAKE_CASE`
- CSS 클래스: `kebab-case`, 하이픈은 단어 사이에 1개만 사용한다 (예: `health-bar-fill`, `result-badge-win`). `_`(언더바), `__`/`--`(BEM 스타일 구분자) 모두 금지. 상태는 별도 클래스로 추가 (예: `tabs-btn is-active`).
- CSS 규칙은 한 줄로 쓴다: `.selector { prop: value; prop: value; }`
- 선택자는 **화면 전용 클래스**(`landing-*`, `battle-*`, `site-header-*`처럼 페이지/영역 접두어가 붙은 클래스)는 실제 DOM의 부모 클래스를 최상단부터 전부 체이닝한다 (예: `.site-header .site-header-logo img`). 반대로 **여러 화면에서 재사용하는 공용 클래스**(`.btn`, `.card`, `.tabs`, `.tabs-btn`, `.page-title`, `.page-desc`, `.hint-text`, `.dom-tree`, `.css-editor`, `.preview-frame`, `.specificity-table`, `.result-badge`, `.health-bar` 등)는 체이닝하지 않고 클래스 단독으로 쓴다 — 체이닝하면 다른 화면에서 재사용할 때마다 그 화면 부모로 규칙을 또 써야 하기 때문.
- localStorage 키: `store.js`에서만 직접 접근. 다른 파일은 반드시 store 함수를 통해서만 읽고 쓴다.

---

## 3. 구조 규칙
- div를 남발하지 않는다.
- 구조는 section, nav, figure 등 다양한 구조로 구현하고, HTML 유효성 검사에 통과해야 한다.
- 유효성 검사 링크 : https://validator.w3.org/
- 코드 들여쓰기는 4로 통일한다.
- 반응형: `max-width: 1024px` 단일 breakpoint로 레이아웃을 전환하고, 최소 360px 너비까지 레이아웃이 깨지지 않아야 한다. 표처럼 가로로 넓은 요소는 `.table-scroll`(overflow-x: auto)로 감싼다.

---

## 4. 모듈 규칙 (ES Modules)

- `<script type="module" src="js/main.js">` 하나만 index.html에 로드. 나머지는 전부 `import`/`export`로 연결.
- 전역 변수(`window.x = ...`)로 상태를 흘리지 않는다. 화면 간 공유가 필요한 값은 반드시 `store.js`를 거친다.
- 한 파일 = 한 역할. `pages/battle.js`에 특이도 계산 로직을 직접 넣지 말고 `core/specificity.js`를 import해서 쓴다.

---

## 5. 상태관리 패턴

```js
// store.js 사용 예시
import { getState, setState, subscribe } from './store.js';

subscribe((state) => {
  // state 변경될 때마다 화면 갱신
});

setState({ battleRecords: [...getState().battleRecords, newRecord] });
```

- `store.js`는 내부적으로 `setState` 호출 시 자동으로 localStorage에 저장한다.
- 각 `pages/*.js`는 진입 시 필요한 상태를 `getState()`로 읽고, 화면을 벗어날 때 이벤트 리스너를 정리(clean up)한다.

---

## 6. 라우팅 규칙

- 라우트 정의는 `router.js` 한 곳에서만 관리한다.
- 라우트: `#`(랜딩), `#battle`, `#typing`, `#diagnose`, `#quest`, `#report` (슬래시 없음)
- 각 페이지 모듈은 `render(container)` 함수 하나를 export하는 것으로 통일한다.

```js
// pages/battle.js
export function render(container) {
  container.innerHTML = `...`;
  // 이벤트 바인딩
}
```

---

## 7. 주석 규칙

- 기본은 주석 없이, 이름만 보고 이해되게 짠다.
- 주석은 **"왜 이렇게 했는지"가 코드만 보고는 알 수 없을 때만** 작성한다 (예: 브라우저 특이도 계산과 다른 근사치를 쓰는 이유).
- "무엇을 하는지" 설명하는 주석은 간략하게 한 줄 이내로 작성한다. TODO 잡담성 주석은 넣지 않는다.

---

## 8. 기타

- 외부 라이브러리는 꼭 필요할 때만 CDN `<script>`로 추가하고, 이 문서에 왜 추가했는지 한 줄 남긴다. (예: Chart.js CDN)
- 브라우저 호환성은 최신 Chrome/Edge 기준으로만 확인한다.
- 커밋 단위는 화면/기능 단위로 나눈다 (예: "배틀 모드 결과 화면 구현").
